#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
	
    int n,sum;
    scanf("%d", &n);
    while(n>0){
    int remainder = n % 10;
    sum = sum + remainder;
    n=n/10;
    }
   
   printf("%d",sum);
    return 0;
}
